<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.layfolk.chat.mapper.AvatarMapper">

    <select id="queryPageAvatar" resultType="Avatar" parameterType="Integer">
        SELECT * FROM diamand.avatar LIMIT #{end} OFFSET 0
    </select>

</mapper>